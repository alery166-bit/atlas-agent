# W20～W26 远程部署与验证记录（2026-08-04）

## 部署结论

W20～W26 当日代码已部署至 `10.210.0.62`，运营端为
`http://10.210.0.62:8300/`，服务端为 `http://10.210.0.62:8301/`。
PostgreSQL、Elasticsearch、服务端和运营端容器均正常运行，服务端健康检查为 `UP`。

数据库由 V9 自动升级至 V13，依次完成配置版本管理、规则回放、连接器验证和黄金样本验收四项迁移。
迁移为增量升级，部署前已有 8 条任务及其审计记录均保留。

## 部署保护

部署前在服务器生成以下备份：

- `/opt/atlas-agent/backups/postgres-pre-w26-20260804-212044.sql.gz`
- `/opt/atlas-agent/backups/source-pre-w26-20260804-212044.tar.gz`

未执行 `docker compose down -v`，未删除 PostgreSQL、ES 数据卷，也未导入或伪造黄金样本。

## 自动化验证

- 后端测试：96/96 通过。
- 运营端测试：9/9 通过。
- 运营端生产构建：13 个路由构建成功。
- Docker 镜像：服务端、运营端均在服务器构建成功。
- Flyway：成功校验 13 个迁移，并从 V9 应用 V10～V13。
- 运行能力：ES、Tavily、风险评分、DOCX 报告均为 `READY`。

## 远程冒烟测试

- `/actuator/health` 返回 `UP`。
- `/api/platform/operations` 返回 8 条真实任务、18 次搜索调用和 1 条失败任务下钻；未采集的模型调用明确显示“未知”。
- `/api/platform/audit?limit=5` 返回真实任务步骤记录。
- `/api/platform/audit/export?limit=3` 返回 UTF-8 CSV，状态码 200。
- `/api/platform/golden-acceptance/suites` 返回空集合，符合尚未导入业务黄金样本的现状。
- `/`、`/operations`、`/audit`、`/acceptance` 均返回 200。

## 浏览器回归

- 运行监控：真实吞吐、失败步骤、错误码、搜索调用和可重试入口正常展示。
- 审计日志：默认读取 21 条记录；按“北京简熹和食品有限公司”筛选后得到 11 条，其他企业记录被排除；导出后出现成功提示。
- 验收评估：正确显示未登记样本集、20～50 份正式门槛和各项待验收状态。
- 三个新增页面均未发现浏览器错误或警告。

## 未完成项

W26 的技术基础设施已经部署并验证，但正式业务盲测仍未完成。继续推进需要 20～50 组历史旧报告和对应的人工终稿；在收到真实样本前，不以合成数据代替正式验收。
