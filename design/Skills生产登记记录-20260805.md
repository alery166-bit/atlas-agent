# Skills 生产登记记录（2026-08-05）

> 2026-08-06 P3 校正：失败策略、输入输出契约和业务参数没有独立运行时消费者，现已改为内置固定、
> 页面只读，后台也拒绝偏离内置声明。V1 仅允许编辑 Skill 启停和发布依赖。

## 结果

已通过平台幂等初始化接口，将 Atlas V1 实际工作流中的 5 个内置 Skill 登记到
`10.210.0.62` 的 DEV 环境配置库：

| 配置键 | 名称 | 内置执行器 | 当前状态 |
|---|---|---|---|
| `skill.company.resolve` | 企业主体识别 | `builtin.company.resolve.v1` | DRAFT |
| `skill.company.snapshot` | 企业数据快照 | `builtin.company.snapshot.v1` | DRAFT |
| `skill.intelligence.search` | 公开信息检索 | `builtin.intelligence.search.v1` | DRAFT |
| `skill.risk.score` | 风险评分 | `builtin.risk.score.v1` | DRAFT |
| `skill.report.generate` | DOCX 报告生成 | `builtin.report.generate.v1` | DRAFT |

登记操作人为 `codex-deploy`。初始化接口只补齐缺失配置，不覆盖已有定义或版本。

## 页面复验

`http://10.210.0.62:8300/skills` 已显示 5 个登记项，并能查看或编辑启停状态、失败策略、
输入输出契约、业务参数和依赖；内置 Java 执行器保持不可替换。浏览器复验未发现错误或警告。

## 发布边界

本次只完成登记，没有发布生效。5 个版本当前均为 V1 草稿；待确认后应逐项执行契约校验和发布。
发布后只影响新建任务，历史任务继续使用已冻结配置。
