# Atlas V1 需求追溯矩阵

基线日期：2026-08-05  
范围：单企业旧风险报告更新。  
状态口径：E0 未开始、E1 已实现、E2 技术验证、E3 集成验证、E4 业务验收、E5 生产就绪。

本矩阵是 G0“需求可追溯”验收门的基线。状态只表示当前取得的最高可信层级；存在“部分”时，
不得把该行计入业务完成项。

| ID | V1 需求 | 主要实现位置 | 自动化/运行证据 | 当前级别 | 仍缺证据或实现 |
|---|---|---|---|---|---|
| R01 | 只做单企业旧风险报告更新 | `AgentRequestParser`、`TaskController`、`RiskReportUpdateWorkflow` | `AgentRequestParserTest`、`TaskApiIntegrationTest`；P1 正式任务与报告 | E4 | 扩大到黄金样本集验证稳定性 |
| R02 | 必须上传既有 DOCX，查询失败停止 | `PreviousReportUploadController`、`LocalPreviousReportUploadStore`、`RiskReportUpdateWorkflow` | `PreviousReportUploadApiIntegrationTest`、`TaskSourceFailureIntegrationTest`；P1 页面上传 | E4（单样本） | 黄金样本中的失败路径与更多模板差异 |
| R03 | 企业主体必须唯一识别，歧义时等待人工 | `CompanyDataProvider`、`RiskReportUpdateWorkflow` 主体解析与确认 | ES Adapter 测试、`TaskApiIntegrationTest` | E3（部分） | 正式样本主体正确率 100% |
| R04 | 开发期离线/JSON，生产 ES；ES 失败不解释为无风险 | `atlas-adapter-offline`、`atlas-adapter-es`、源失败状态机 | 100 企业 ES 验证、`ElasticsearchCompanyDataProviderTest` | E3（开发） | 生产 ES 安全、容量和恢复 |
| R05 | 舆情使用全称、简称、品牌、门店等身份词 | `PublicSearchQueryPlanner`、`CompanyAliasApplicationService`、Tavily Provider | `PublicSearchQueryPlannerTest`、W18 真实检索记录 | E3（部分） | 黄金样本准确率/召回率 |
| R06 | 舆情必须人工确认或排除并保留依据 | 证据决定 API、`EvidenceDecision`、`operator_confirmation` | `PublicIntelligenceApiIntegrationTest`、审计接口 | E2 | 一份真实任务的逐条运营决定 |
| R07 | 保留输入旧报告分、ES旧分、规则分、原始分、人工分及原因 | `TaskWorkspaceApplicationService`、`RiskScoreApplicationService`、`JdbcRiskScoreSnapshotRepository`、任务工作台 | `PreviousReportUploadApiIntegrationTest`、`RiskScoreApiIntegrationTest`；P1真实任务页面复验 | E3 | 真实非零调分样本与终稿回溯 |
| R08 | 高/中高/中/中低/低阈值为 8/6/4/2 | `RiskLevel`、版本化规则文档 | `RiskLevelTest`、`VersionedRiskScoreEngineTest` | E2 | 黄金回放与业务确认 |
| R09 | 已确认失联/欠薪/闭店最低 6/6/8 分 | `VersionedRiskScoreEngine` 事件保底 | 领域测试、规则 API 测试 | E2 | 证据不足和多事件真实样本 |
| R10 | 不丢弃旧评分模型，完整迁移旧规则与标签 | `LegacyPureScoreCalculator`、`LegacyRiskScoreAdapter`、`LegacyRiskTraceabilityCatalog`、旧代码资产 | 39项风险字典及15组规则线上追溯；全量测试；P2浏览器复验 | E3（追溯完成、执行部分） | 分支/总部、朝阳减分、关联分上游、日期缺陷和历史标签编号仍待确认/实现；黄金样本等价性未验收 |
| R11 | V1 使用正式模板并输出 DOCX | `OoxmlReportTemplateRenderer`、`ReportApplicationService` | DOCX 单元/API 测试；P1 正式模板生成、Word 业务与版式确认 | E4（单样本） | 20～50 份黄金样本结构稳定性 |
| R12 | 暂无审核分级，操作人隔离且行为可审计 | 任务查询、操作人请求头、`PlatformObservabilityPort` 审计 | 任务/审计集成测试 | E2～E3 | 统一身份接入和越权验证 |
| R13 | 配置发布只影响新任务，历史任务保留快照 | 配置版本、环境绑定、任务配置快照；仅冻结有运行消费者的类别 | 搜索配置任务冻结执行测试、规则/Skill/模板集成测试；Tavily V1/V3 DEV 双任务冻结差异及回滚实证 | E3 | 黄金样本阶段结合真实执行继续验证 |
| R14 | 页面可编辑项必须真正控制执行 | P3 字段消费审计、后台固定值/发布门禁、模板定位词校验 | P3 相关12项后端集成测试、运营端15项契约测试；DEV 部署后五类管理页浏览器复验 | E3 | 真实业务人员持续使用反馈；ES/模型接入运行时后再开放发布 |
| R15 | 监控不得把停滞任务显示为活跃 | `JdbcPlatformObservabilityRepository`、运行监控页 | `PlatformObservabilityApiIntegrationTest`；15 分钟阈值 | E2 | DEV 部署后的实际停滞任务复验 |
| R16 | 20～50 份真实历史报告/终稿盲测 | `GoldenAcceptanceService`、验收评估页 | `GoldenAcceptanceApiIntegrationTest` | E1 | 真实样本清单与运营盲测 |
| R17 | 生产具备身份、安全、容量、备份恢复和回滚 | Docker/部署记录 | 仅 DEV 备份与回滚记录 | E0～E1 | W27 全部生产验收证据 |

## G0 判定

- 17 项 V1 需求均已登记到实现、测试和缺口，G0 基线建立完成。
- 本矩阵不表示 17 项需求已经完成；当前主要集中在 E1～E3。
- 每次代码或验收状态变化必须同步本矩阵。没有新增证据时，不提高等级。
