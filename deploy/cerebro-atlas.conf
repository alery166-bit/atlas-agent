include "application.conf"

# The secret and basic-auth credentials are injected by the deployment script.
secret = ${CEREBRO_SECRET}

hosts = [
  {
    host = "http://elasticsearch-dev:9200"
    name = "Atlas ES 9.4.2"
  }
]
